"use strict";
function test() {
    var vector = new Vector(0, 0);
    //let output = Document.
    console.log(vector.x, vector.y);
    vector.x += 5;
    vector.y -= 10;
    console.log(vector.x, vector.y);
}
test();
